package SpaceMarine;

import java.util.Scanner;

/**
 * This interface imposes an execute method on the objects of each class that
 * implements it
 */
public interface Executable {
    void execute(Scanner sc);
}
